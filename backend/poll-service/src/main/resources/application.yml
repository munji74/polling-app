server:
  port: 8082

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/polldb
    username: app
    password: app
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect

app:
  jwt:
    secret: Genie1Out2The3Bottle100%0  # MUST MATCH auth-service
