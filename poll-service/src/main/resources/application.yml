server:
  port: 8082

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/polldb
    username: app
    password: app
  jpa:
    hibernate.ddl-auto: update
    open-in-view: false
    properties:
      hibernate.format_sql: true

# IMPORTANT: paste the EXACT same secret from auth-service here
jwt:
  secret: "PASTE-THE-SAME-64+ BYTE SECRET AS AUTH-SERVICE"
